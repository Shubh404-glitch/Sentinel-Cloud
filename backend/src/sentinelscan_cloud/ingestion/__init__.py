"""Report Import Flow (Section 11): schema validation, parsers,
normalization, and the ingestion workflow that ties them together.
Nothing in this package ever executes anything found inside an
imported report -- a Report is always treated as inert data
(Section 4, Section 15)."""
