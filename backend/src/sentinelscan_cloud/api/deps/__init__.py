"""FastAPI dependencies -- the enforcement points for authentication and
authorization (Section 9: the API layer validates input shape,
"enforces tenant scoping and auth", and delegates everything else)."""
from __future__ import annotations
